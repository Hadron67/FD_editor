<resources>

    <style name="AppTheme" parent="Theme.FDeditor">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="Theme.FDeditor.NoTitleBar" parent="android:Theme.Holo.NoActionBar">
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="Theme.FDeditor" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="actionBarStyle">@style/ActionBar</item>
        <item name="windowActionBarOverlay">true</item>
        <item name="windowActionModeOverlay">true</item>
        <item name="actionModeBackground">@drawable/actionmode_background</item>

        <item name="android:alertDialogStyle">@style/exportdialogue_style</item>

        <item name="colorPrimaryDark">@color/primary_dark_material_dark</item>
        <item name="colorPrimary">@color/primary_material_dark</item>

    </style>

    <style name="Theme.FDeditor.List" parent="android:Widget.AbsListView">
        <item name="android:listSelector">@drawable/list_item_background</item>
        <item name="android:divider">@android:color/transparent</item>
        <item name="android:dividerHeight">0dp</item>
    </style>

    <style name="ActionBar" parent="Widget.AppCompat.Light.ActionBar">
        <item name="titleTextStyle">@style/ActionBar.Text</item>
    </style>

    <style name="ActionBar.Text" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">#ffffff</item>
    </style>

    <style name="ActionBar.ArrowToggle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="color">#ffffff</item>
    </style>

    <style name="Toolbar.FDeditor" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>


    <style name="tool_box_style" parent="android:Widget.ListPopupWindow">
        <item name="android:windowIsFloating">true</item>
        <item name="android:popupBackground">@drawable/toolbox_background</item>
        <item name="android:shadowRadius">5</item>
        <item name="android:shadowDx">5</item>
        <item name="android:shadowDy">5</item>
        <item name="android:shadowColor">#183941</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <style name="exportdialogue_style" parent="android:style/Theme.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@drawable/exportimagedialogue_background</item>
    </style>

</resources>
